First display the para

Underline the current letter of the current word
(We have to track current word and inside the word current letter)

when user presses space change the current word to next word

when user types with each ke stroke the current letter index increments to one 
and when presses backspace current letter index decrements to one

